# Area of rectangle = length * width
length = float(input("Enter length = "))
width = float(input("Enter width = "))
area = length * width
print(f"Area of rectangle = {area}")
perametar = 2 * (length + width)
print(f'Perametar = {perametar}')
